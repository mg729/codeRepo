#include<bits/stdc++.h>
using namespace std;

int n, val;
vector<int> v;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	freopen("input.txt", "rt", stdin);
	
	for(int i = 0; i < n; i++) {
		cin >> val;
		v.push_back(val);
	}
	
	bool isEqualSum = true;
	for(int i = 0; i < n; i++) {
		
	}
	
	if(isEqualSum) cout << "YES";
	else cout << "NO";
	
	return 0;
}